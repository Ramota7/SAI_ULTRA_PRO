Esta carpeta almacena backups automáticos de config.json y logs diarios generados por el sistema unicornio.

- Los archivos config.json se respaldan con timestamp cada vez que cambia el umbral de racha de pérdidas.
- Los logs diarios se copian automáticamente al finalizar cada ciclo.
