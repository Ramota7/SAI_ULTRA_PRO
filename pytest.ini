[pytest]
# Evitar recoger tests internos que comparten nombres con tests/ en la raíz
norecursedirs = sai_ultra_pro/tests
python_files = test_*.py
addopts = -ra
filterwarnings = ignore::DeprecationWarning